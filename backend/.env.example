JWT_SECRET_KEY=change-me-in-production
DATABASE_URL=sqlite:///./app.db
# Firebase 콘솔 > 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성 으로 받은 JSON 경로.
# 비워두면 FCM 발송은 로그만 남기고 스킵됨(로컬 개발 시 기본값).
FIREBASE_CREDENTIALS_PATH=
